export enum Prioridad {
  BAJA = 'baja',
  MEDIA = 'media',
  ALTA = 'alta',
  CRITICA = 'critica',
}

export enum EstadoIncidencia {
  ABIERTA = 'abierta',
  EN_PROGRESO = 'en_progreso',
  RESUELTA = 'resuelta',
  CERRADA = 'cerrada',
}
